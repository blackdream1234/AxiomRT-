# axiom-fuzz — deterministic host fuzz infrastructure (AXIOM-ROBUST-002).
# Host-side tool; std only and zero external dependencies.

[package]
name = "axiom-fuzz"
version = "0.1.0"
edition = "2021"

[dependencies]
