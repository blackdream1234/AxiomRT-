# axiom-fuzz — deterministic host fuzz infrastructure (AXIOM-ROBUST-002).
# Host-side tool; std only, using the local kernel model as source of truth.

[package]
name = "axiom-fuzz"
version = "0.1.0"
edition = "2021"

[dependencies]
kernel = { path = "../../kernel" }
